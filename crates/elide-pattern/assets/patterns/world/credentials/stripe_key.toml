# Stripe secret API key: `sk_live_` or `sk_test_` followed by the
# 24+ char key body.

name = "stripe-key"
label = "api_key"

[[variants]]
regex = "\\bsk_(live|test)_[a-zA-Z0-9]{24,}\\b"
score = 0.95
