# HTTP(S) URL: an `http://` or `https://` scheme followed by a
# host and path, stopping at whitespace.

name = "url"
label = "url"

[[variants]]
regex = "\\bhttps?://[^\\s/$.?#][^\\s]*\\b"
score = 0.5
