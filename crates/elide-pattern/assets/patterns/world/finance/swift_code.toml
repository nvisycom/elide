# SWIFT / BIC code: 4-letter bank code, 2-letter ISO country code,
# 2-char location code, and an optional 3-char branch code.

name = "swift-code"
label = "swift_code"

[[variants]]
regex = "\\b[A-Z]{4}[A-Z]{2}[A-Z0-9]{2}(?:[A-Z0-9]{3})?\\b"
score = 0.5
