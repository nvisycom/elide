# Ethereum address: `0x` followed by 40 hex characters (a 20-byte
# account or contract address).

name = "ethereum-address"
label = "crypto_address"

[[variants]]
regex = "\\b0x[0-9a-fA-F]{40}\\b"
score = 0.5
