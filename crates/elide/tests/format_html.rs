//! End-to-end html codec suite. Scenarios live under `format_html/`; add
//! one by dropping a file there and a `mod` line in `format_html/mod.rs`.

#[path = "support/mod.rs"]
mod support;

#[path = "format_html/mod.rs"]
mod format_html;
