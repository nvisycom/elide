# Configuration for cargo-deny
# See: https://embarkstudios.github.io/cargo-deny/

[graph]
targets = [
    { triple = "x86_64-unknown-linux-gnu" },
    { triple = "x86_64-unknown-linux-musl" },
    { triple = "x86_64-apple-darwin" },
    { triple = "aarch64-apple-darwin" },
    { triple = "x86_64-pc-windows-msvc" },
]

[advisories]
db-path = "~/.cargo/advisory-db"
db-urls = ["https://github.com/rustsec/advisory-db"]
unmaintained = "all"
yanked = "deny"
ignore = []

[licenses]
confidence-threshold = 0.8
private = { ignore = false, registries = [] }
unused-allowed-license = "warn"

allow = [
    "MIT",
    "MIT-0",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "MPL-2.0",
    "Unicode-3.0",
    "Unlicense",
    "BSL-1.0",
    "CC0-1.0",
    "Zlib",
]

# LGPL-3.0, accepted only for the opt-in `mp3` codec feature (off by default,
# so absent from the graph deny checks by default; surfaces under
# `--all-features`). MP3 *encoding* has no permissively-licensed, production
# quality alternative in the ecosystem — LAME (libmp3lame) is effectively the
# only option, and every substitute (incl. Shine) is also LGPL. The obligation
# is satisfiable for proprietary use via dynamic/separate linking.
exceptions = [
    { name = "mp3lame-encoder", allow = ["LGPL-3.0"] },
    { name = "mp3lame-sys", allow = ["LGPL-3.0"] },
]

[bans]
multiple-versions = "warn"
wildcards = "deny"
highlight = "all"

allow = []

deny = [
    { name = "openssl-sys", version = "*" },
    { name = "openssl", version = "*" },
]

skip = []
skip-tree = []
# Self-referential dev-dependency for test feature activation is not a real wildcard.
allow-wildcard-paths = true

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
